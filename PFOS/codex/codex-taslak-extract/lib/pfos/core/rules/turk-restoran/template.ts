/**
 * Türk Restoranı — Sütiş Şişhane referans listesi (200–5000 m²).
 * Eski senkron S13 şablonu kaldırıldı; teklif için resolveTemplateForQuote kullanın.
 */
export { buildTurkRestoranTemplate } from "@/lib/pfos/referans/turk-restoran";
