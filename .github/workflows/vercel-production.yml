# DEVRE DIŞI — Vercel production deploy kullanılmıyor.
# Canlı: Hetzner + Docker → E-TICARET/site/docs/HETZNER-DEPLOY.md
#
# Vercel Git entegrasyonunu da kapatın:
# vercel.com → equsto projesi → Settings → Git → Disconnect
#
# Eski GHA deploy (secret'lar varsa) yalnızca elle tetiklenebilir; job çalışmaz.

name: Vercel Production (disabled)

on:
  workflow_dispatch:

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel deploy devre dışı. Hetzner kullanın."
