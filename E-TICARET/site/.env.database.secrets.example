# Kopyala: copy .env.database.secrets.example .env.database.secrets
# Vercel → equsto → Settings → Environment Variables → Production
# DATABASE_URL ve DIRECT_URL değerlerini yapıştırın (tırnak YOK)
# Kaydedin; asistan: node scripts/push-db-env-hetzner.mjs .env.database.secrets

DATABASE_URL=
DIRECT_URL=
