# Supabase — lxwwbuvbaejtlqmzoykx (Frankfurt / eu-central-1, TR'ye yakın)
# Kopyala:  copy .env.local.template .env.local
# YOUR_DB_PASSWORD → Database password (Connect / Settings → Database)

# Dashboard → Database → Connection string → bölgeyi birebir kopyalayın (aws-1-eu-central-1)
# Transaction pooler (6543) — runtime
DATABASE_URL="postgresql://postgres.lxwwbuvbaejtlqmzoykx:YOUR_DB_PASSWORD@aws-1-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

DIRECT_URL="postgresql://postgres.lxwwbuvbaejtlqmzoykx:YOUR_DB_PASSWORD@aws-1-eu-central-1.pooler.supabase.com:5432/postgres"

# Admin panel + API (kendiniz üretin: eq_adm_ + rastgele)
EQUSTO_ADMIN_BEARER="equsto-pro-1d4fc58e4eb081ab423cfce48163d3573288734b0cfa5353"

# Telegram bildirim — docs/BILDIRIM-KURULUM.md
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

NEXT_PUBLIC_SITE_URL="http://localhost:3099"

# Meilisearch — yerel Docker: npm run search:up  (docs/MEILISEARCH.md)
MEILISEARCH_HOST="http://127.0.0.1:7700"
MEILISEARCH_MASTER_KEY="equsto-dev-meili-key"
MEILISEARCH_INDEX="equsto_products"

# Geçiş dönemi
LEGACY_DATA_BASE="https://equsto.com"

# AWS S3 + CloudFront — Faz B (docs/FAZ-B-AWS.md)
# AWS_S3_BUCKET=equsto-assets
# AWS_REGION=eu-central-1
# AWS_PROFILE=equsto
# NEXT_PUBLIC_ASSET_CDN_URL=https://d1234abcd.cloudfront.net

# Üye girişi — Google (OAuth 2.0 Client ID, Web application)
# Google Cloud Console → APIs & Services → Credentials → Create OAuth client ID
# Authorized JavaScript origins: https://equsto.com  http://localhost:3099
# Authorized redirect URIs: (GSI One Tap için genelde gerekmez; popup kullanılıyor)
# Canlı: Vercel → equsto → Settings → Environment Variables (Production + Preview)
GOOGLE_CLIENT_ID=""
# Alternatif isimler (kod hepsini okur): EQUSTO_GOOGLE_CLIENT_ID, NEXT_PUBLIC_GOOGLE_CLIENT_ID

# PDF import / PFOS liste yükle (Claude) — console.anthropic.com → API Keys
# Canlı: Vercel → Environment Variables → ANTHROPIC_API_KEY
ANTHROPIC_API_KEY=""
# ANTHROPIC_MODEL=claude-sonnet-4-6

# İleride Auth (şimdilik opsiyonel — Framework ekranındaki değerler)
# Supabase Auth — Dashboard → Settings → API → anon public (veya Publishable key)
NEXT_PUBLIC_SUPABASE_URL=https://lxwwbuvbaejtlqmzoykx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...veya_sb_publishable_...

$env:NODE_OPTIONS="--max-old-space-size=2048"
